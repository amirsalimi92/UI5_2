<mvc:View xmlns="sap.m" 
        xmlns:mvc="sap.ui.core.mvc" 
        controllerName="myapp.controller.App"
        displayBlock="true">

    <Shell>
        <App class="myAppDemoWT">
            <pages>
                <Page title="{i18n>viewAppPageTitle}">
                    <headerContent>
                            <Button icon="sap-icon://touch"
                            press=".onShowHelloDialog"/>
                    </headerContent>
                    <content>
                        <mvc:XMLView viewName="myapp.view.helloPanel">
                            
                        </mvc:XMLView>
                    </content>
                </Page>
            </pages>
        </App>
    </Shell>

        <Text text="{i18n>viewAppBottom}" />

</mvc:View>